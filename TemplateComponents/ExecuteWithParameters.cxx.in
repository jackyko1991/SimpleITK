$(if members and #members > 0 then
OUT = [[

Image ${name}ImageFilter::Execute ( $(include ImageParameters.in)$(foreach members $(if number_of_inputs > 0 then OUT="," end)
  ${type} in${name}) )
{
$(foreach members
$(if (not no_set_method) or (no_set_method == 0) then
OUT = '  this->Set${name} ( in${name} );'
end)
)
  return this->Execute ( image1 $(if number_of_inputs==2 then OUT=', image2' end) );
}
]]
end)
